<!--
GENERATED FROM LEDGER DATA. Do not edit this file by hand as if it were the template —
edit the ledger and regenerate instead. Every {{token}} below is filled in from a
ledger.json (and, where noted, from `dashboard.py drift` / `rollback-preview` output) by
whoever or whatever produces the filled-in copy. See references/DASHBOARD.md, "Generating
the degraded text view", for the procedure. Delete this comment once the tokens below are
filled in.
-->

# Dashboard (text view)

Generated from `ledger.json`. This is the no-browser equivalent of the generated
`dashboard.html`: same source data, same nine panels, no script and no interactivity.

## Overview

{{overview_counts}}

Known projects:

{{overview_projects}}

## Inventory

What the current baseline says is present, and where each item came from.

{{inventory_rows}}

## Changes

What each RUN record touched, claimed, and self-reported.

{{changes_rows}}

## Provenance

For each touched anchor or key: which runs touched it, through which proposal, back to
which material and evidence.

{{provenance_chains}}

## Rollback

What undoing each RUN would restore, leave alone, or fail to restore, and whether its
backup is trustworthy.

{{rollback_previews}}

## Backlog

Deferred findings, their reason, and what brings each one back.

{{backlog_rows}}

## Decisions

Every ADR record. Nothing here is ever filtered out, including superseded ones.

{{decisions_rows}}

## Materials

Every MATERIAL record, its classification, and which evidence has already expired.

{{materials_rows}}

## Help

Operating states: ANALYSIS, DELIBERATION, IMPLEMENTATION.

Classifications: ADOPT GLOBALLY, ADOPT AS A DEFAULT FOR NEW PROJECTS, MIGRATE EXISTING
PROJECTS, ADOPT LOCALLY, TEST IN ISOLATION, ADAPT, MONITOR, REJECT, OBSOLETE, ALREADY
IMPLEMENTED, NOT APPLICABLE, NEEDS MORE EVIDENCE, RISK EXCEEDS BENEFIT.

Record statuses: ANALYZED, PROPOSED, DECIDED, IMPLEMENTED, VALIDATED, VALIDATED WITH
CAVEATS, NOT IMPLEMENTED, PENDING, ROLLBACK, SUPERSEDED.

Run results: VALIDATED, VALIDATED WITH CAVEATS, PARTIALLY VALIDATED, NOT VALIDATED,
FAILED, ROLLBACK COMPLETED.

Commands:

- `dashboard.py verify LEDGER` — check a ledger against the schema and against itself.
- `dashboard.py scan CLIENT --project ROOT` — record what a client currently has, as a
  baseline, without changing anything.
- `dashboard.py drift LEDGER` — compare every recorded item and run target against the
  files on disk now.
- `dashboard.py rollback-preview LEDGER RUN-ID` — show what undoing one run would
  restore, leave alone, and fail to restore.
- `dashboard.py build LEDGER` — generate the HTML dashboard from a ledger. Refuses any
  ledger that `verify` would reject.
